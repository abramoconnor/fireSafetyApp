import React, { Fragment } from 'react';
import Home from './Home';

// possible a useless file that culd be replaced with direct route to home
export default function Dashboard() {
  return (
    <Fragment>
      <Home/>
    </Fragment>
  );
}
